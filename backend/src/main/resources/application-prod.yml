spring:
  application:
    name: terraformers-backend-modernization
  autoconfigure:
    exclude: []
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MariaDBDialect
    show-sql: false
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 12MB
  lifecycle:
    timeout-per-shutdown-phase: 20s

server:
  port: 8080
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  prometheus:
    metrics:
      export:
        enabled: true
  cloudwatch:
    metrics:
      export:
        enabled: true
        namespace: Terraformers/Backend
        step: 1m
  endpoint:
    health:
      probes:
        enabled: true

terraformers:
  runtime:
    required-env:
      - SPRING_DATASOURCE_URL
      - SPRING_DATASOURCE_USERNAME
      - SPRING_DATASOURCE_PASSWORD
      - COGNITO_REGION
      - COGNITO_USER_POOL_ID
      - COGNITO_USER_POOL_CLIENT_ID
      - COGNITO_JWKS_URL
      - S3_BUCKET_NAME
  security:
    jwt:
      enabled: true
      issuer-uri: https://cognito-idp.${COGNITO_REGION}.amazonaws.com/${COGNITO_USER_POOL_ID}
      jwk-set-uri: ${COGNITO_JWKS_URL}
      client-id: ${COGNITO_USER_POOL_CLIENT_ID}
  upload:
    source-bucket: ${S3_BUCKET_NAME}
    source-prefix: browser-uploads
  storage:
    s3-reader-enabled: ${S3_READER_ENABLED:false}
    s3-writer-enabled: ${S3_WRITER_ENABLED:false}
  analysis:
    mode: integrated-java
    # Generic selection wins when ANALYSIS_PROVIDER is set. The legacy switch remains an M1-7 compatibility fallback.
    provider: ${ANALYSIS_PROVIDER:}
    embedding-provider: ${EMBEDDING_PROVIDER:bedrock}
    bedrock-provider-enabled: ${BEDROCK_PROVIDER_ENABLED:false}
    retrieval-mode: ${RETRIEVAL_MODE:DISABLED}
    bedrock:
      model-id: ${BEDROCK_MODEL_ID:}
      embedding-model-id: ${BEDROCK_EMBEDDING_MODEL_ID:}
      max-tokens: ${BEDROCK_MAX_TOKENS:8192}
    opensearch-endpoint: ${OPENSEARCH_ENDPOINT:}
    opensearch-service-name: ${OPENSEARCH_SERVICE_NAME:aoss}
    opensearch-top-k: ${OPENSEARCH_TOP_K:3}
    index-name: ${INDEX_NAME:}
    vector-field-name: ${VECTOR_FIELD_NAME:}
    content-field-name: ${CONTENT_FIELD_NAME:}
    corpus-version: ${CORPUS_VERSION:terraformers-reference-v1}
    provider-version: ${PROVIDER_VERSION:5.100.0}
    expected-vector-dimension: ${EXPECTED_VECTOR_DIMENSION:}
    result-bucket-name: ${ANALYSIS_RESULT_BUCKET_NAME:${S3_BUCKET_NAME}}
    result-key-prefix: ${ANALYSIS_RESULT_KEY_PREFIX:analysis-results}
    sqs-publisher-enabled: ${ANALYSIS_SQS_PUBLISHER_ENABLED:false}
    progress-queue-url: ${AI_LOG_QUEUE_URL:}
    result-queue-url: ${TERRAFORM_LOG_QUEUE_URL:}

info:
  build:
    source-revision: ${BUILD_SOURCE_REVISION:unknown}
